#!/usr/bin/python

import fnmatch
import os

image = ['*.jpg', '*.jpeg', '*.png', '*.tif', '*.tiff']
matches = []

for root, dirnames, fineames in os.walk("C:\"):
    for extenstions in images:
        for filenames in fnamtch.filter9filternames, extensions):
            matches.append(os.path.join(root, filename))

