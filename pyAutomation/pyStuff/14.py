#!/usr/bin/python
# Removing Duplicates
# Remove dupicates from list 

a = ["1", 1, "1", 2]
a = list(set(a))
print(a)


# Set function to covert the list to a set which would remove dups
# Then use list to set back to a list

