#!/usr/bin/python
# Create script tht generates the expected otput using my_range as input data.
# expected list output are all strings

my_range = range(1, 21)
print(list(map(str, my_range)))

# Map function, applies a function to interval object.  my_range is an object
# that you can interate
# it applies str function, coverts numbers to strings


