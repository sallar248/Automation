#!/usr/bin/python
# Create dictionary with keys with values
# Print out dictionary


from pprint import pprint
d = dict(a = list(range(1,11)), b = list(range(11,21)), c = list(range(21,33)))
pprint(d)

# Ranges are used to construct the lists
# Use pprint module which is used ot print out well formatted views of 
# datatypes in Python

